import styled from "@emotion/styled"

const Wrapper = styled.div`
    box-sizing: border-box;
    font-size: 100px;
    color: white;
`

export default function MainRendingPage(){
    return(
        <Wrapper>
            메인 페이지🎈
        </Wrapper>
    )
}